TASK 2 P1 EXECUTION EVIDENCE
=================================

CURRENT STATUS POINTER
----------------------

The historical dispositions below are preserved as evidence of their own
execution windows. The latest authoritative result is the final continuation
headed "CONTINUATION — FINAL SINGLE-HOST RUNTIME AND P2 HANDOFF,
2026-09-24" at the end of this file.

FINAL DISPOSITION

TASK2-P1 PARTIAL —
RUNTIME OR HOST EXPOSURE GATE PENDING

CP1: INCOMPLETE / BLOCKED
CP2: NOT RUN
P2: NOT AUTHORIZED TO START

The verified Keycloak 26.7.4 manifest resolved, but its final image layer did
not complete during several bounded cached pulls. No unverified image was used
and no Stage A resource was created. Stage B and realm provisioning were not
attempted.

STARTING GATE

Opening gate executed before: 2026-09-23T13:39:12Z
Exact opening-gate timestamp: not separately captured
Expected branch: temp/work-in-progress
Actual branch: temp/work-in-progress
Expected reviewed checkpoint: 1fea0957be5437948467d6e9470418d1d7523810
Actual starting HEAD: 1fea0957be5437948467d6e9470418d1d7523810
Commit subject: wip: task2-p0-patch
Starting git status --short --untracked-files=all: <empty; clean>
Ancestry command:
git merge-base --is-ancestor
1fea0957be5437948467d6e9470418d1d7523810 HEAD
Exit status: 0
Intervening commits: none; HEAD was the reviewed checkpoint.

REQUIRED SOURCE REVIEW

OBSERVED PASS

Read before implementation:

- task2/README.md
- task2/architecture.md
- task2/threat-model.md
- task2/test-plan.md
- docs/evidence/task2/t2-p0-preflight.txt
- docs/evidence/task2/t2-p0-scope-record.txt
- .gitignore
- .env.example
- docs/case-study-secops-engineer-2026.pdf, including Task 2 Option A

The local PDF is four pages with SHA-256
bf509a32052527444b4fc75c7492c263705606c66fe01e0ce4ff097958c2f6de.
The host lacked pdftotext and other PDF utilities; its embedded compressed text
and Unicode maps were read locally without changing the PDF. Option A requires
centralized identity design for two or three internal applications and a
working OIDC/SSO POC for at least one application. The P0 architecture remains
the technical source of truth.

CURRENT PREFLIGHT

Timestamp: 2026-09-23T13:39:12Z

OBSERVED PASS — Docker client:
Docker version 29.3.0, build 5927d80

OBSERVED PASS — Docker Compose:
Docker Compose version v5.1.0

OBSERVED PASS — daemon and host platform:
ServerVersion=29.3.0
Architecture=x86_64
OSType=linux
KernelVersion=5.15.167.4-microsoft-standard-WSL2
Host: Linux 5.15.167.4-microsoft-standard-WSL2 x86_64

OBSERVED PASS — exact intended names were free before image pulls:
- task2-iam-keycloak-stage-a-1
- task2-iam-keycloak-stage-b-1
- task2-iam-diagnostic-1
- task2-iam-oidc
- task2-iam-keycloak-data

OBSERVED PASS — WSL listener preflight:
ss -ltn 'sport = :18082 or sport = :18083'
returned only the header; no matching WSL listener was present.

OBSERVED PASS — Windows listener preflight:
PowerShell Get-NetTCPConnection reported no listener on 18082 or 18083.

OBSERVED PASS — Windows DNS observation:
Resolve-DnsName returned ::1 and 127.0.0.1 for both auth.localhost and
ops.localhost.

UNVERIFIED — Windows browser behavior:
No service existed to test in an actual browser. DNS behavior is not promoted
to browser evidence.

HISTORICAL REPORTED and RECONFIRMED — WSL CLI DNS:
getent ahostsv4 auth.localhost exited 2 with no result.
getent ahostsv4 ops.localhost exited 2 with no result.
This does not predict Windows browser handling of the special-use suffix.

IMAGE INTEGRITY AND REGISTRY

Timestamp: 2026-09-23T13:39:51Z

Official sources:
- Keycloak image source: official Keycloak container documentation and
  quay.io/keycloak/keycloak
- Diagnostic image source: Docker Official Image docker.io/library/alpine;
  manifest annotation references github.com/alpinelinux/docker-alpine

OBSERVED PASS — registry access and immutable resolution:

Keycloak:
Tag: quay.io/keycloak/keycloak:26.7.4
Index digest:
sha256:82a77884f3af238beab1e7afd63b5f530e1b5c0590bd7aa60b40a40463e29b2c
Selected platform: linux/amd64
Platform manifest digest:
sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf

Diagnostic:
Tag: docker.io/library/alpine:3.22.1
Index digest:
sha256:4bcff63911fcb4448bd4fdacec207030997caf25e9bea4045fa6c8c44de311d1
Selected platform: linux/amd64
Platform manifest digest:
sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f

OBSERVED PASS — diagnostic image local inspection:
Image ID:
sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f
OS=linux
ARCH=amd64
Repo digest:
alpine@sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f

BLOCKED — Keycloak local image transfer:
At 2026-09-23T13:46:39Z,
docker compose --project-directory task2 --profile '*' pull
completed the Alpine image but did not finish the Keycloak image.

Subsequent exact-digest attempts used:
docker pull
quay.io/keycloak/keycloak:26.7.4@sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf

Observed intermediate output included:
- dcee140b22ee: Already exists
- bb7474a486db: Download complete
- bb7474a486db: Pull complete
- b021fe485c81 remained in Pulling fs layer state

No registry error was returned; the blob transfer did not complete within the
bounded attached attempts. Tool-level termination left four exact P1-owned
docker pull client processes. At 2026-09-23T14:17:39Z they were identified by
exact Keycloak digest and terminated with SIGTERM. A following process check
returned no rows.

Final exact image inspection result:
Error response from daemon: No such image:
quay.io/keycloak/keycloak:26.7.4@sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf
Exit status: 1

No alternate tag, floating tag, or unverified image was used. No Docker-wide
cleanup or prune was run.

STATIC CONFIGURATION VALIDATION

OBSERVED PASS — task2/keycloak/ops-realm.json parsed successfully with
python3 -m json.tool.

OBSERVED PASS — docker compose --profile '*' config --quiet exited 0.

Rendered model summary:
Project: task2-iam
Profiles: diagnostic, stage-a, stage-b
Services: diagnostic, keycloak-stage-a, keycloak-stage-b
Network: task2-iam-oidc
Volume: task2-iam-keycloak-data
Images: exact platform-specific digests recorded above

OBSERVED PASS — secret-safe structural assertions confirmed:

- both Keycloak stages use the same digest, linux/amd64, port, environment,
  network alias, persistent volume, capability drop, and no-new-privileges;
- only 127.0.0.1:18082:18082 is published;
- Stage A has no --import-realm;
- Stage B has --import-realm and only the required read-only realm bind mount;
- the diagnostic service has no port, volume, Docker socket, or privilege;
- no service references a Docker socket;
- the diagnostic command has a bounded 600-second lifetime.

OBSERVED PASS — static realm assertions confirmed:

- realm ops is enabled;
- ops-dashboard is confidential, standard flow is on, PKCE S256 is required,
  the redirect URI is exact, and implicit/direct/service-account grants and
  Full Scope Allowed are off;
- asset-inventory and runbook-portal are disabled design-only clients;
- all required client roles, groups, and explicit group-role mappings exist;
- ops-dashboard-roles is the only custom scope in the file and is a default
  scope only for ops-dashboard;
- the User Client Role mapper is restricted to client ID ops-dashboard and
  maps multivalued String claim ops_roles to the ID token only;
- the dedicated scope mapping lists only viewer and admin;
- no JSON key named secret exists.

This is static configuration validation, not imported Keycloak state and not
token-output evidence.

CHECKPOINT 1 RESULTS

R1 — Task 2 isolation:
CONFIGURED / PARTIAL OBSERVED PASS for the rendered model.
Runtime ownership labels: UNVERIFIED because no resource was created.

R2 — Docker runtime:
Daemon and Compose validation: OBSERVED PASS.
Stage A startup: BLOCKED by incomplete verified Keycloak image pull.

R3 — image integrity:
Registry resolution: OBSERVED PASS.
Diagnostic local image/platform: OBSERVED PASS.
Keycloak local image/platform: BLOCKED; exact image absent.

R4 — Keycloak process/listener:
NOT RUN.

R5 — Docker DNS and diagnostic HTTP:
NOT RUN. No diagnostic container was created.

R6 — WSL host path:
NOT RUN. Preflight DNS failure is recorded separately.

R7 — Windows browser:
UNVERIFIED / NOT RUN. No service was available.

R8 — independent external exposure:
UNVERIFIED / NOT RUN. No service was available.

R9 — future Flask port:
OBSERVED PASS for point-in-time WSL and Windows preflight only; no conflict was
shown on 18083. Flask was not created and no response was required.

R10 — admin boundary:
CONFIGURED: no management port is published.
Observed runtime result: NOT RUN.

CP1 DISPOSITION:
INCOMPLETE / BLOCKED.
Realm ops was not provisioned.

STAGE TRANSITION

NOT RUN. Stage A never started. There was no Keycloak container or data volume
to stop or preserve, and port 18082 remained unused by Task 2.

CHECKPOINT 2 RESULTS

Stage B startup/import: NOT RUN
O1 discovery: NOT RUN
O2 advertised endpoints: NOT RUN
O3 JWKS: NOT RUN
O4 actual client configuration: NOT RUN
O5 actual authorization configuration: NOT RUN
O6 ACTUAL ops_roles TOKEN OUTPUT:
UNVERIFIED — MANDATORY P2 ENTRY TEST
O7 realm authorization page: NOT RUN
Optional restart/idempotency:
RESTART/IDEMPOTENCY: NOT VERIFIED

DIAGNOSTIC CLEANUP AND FINAL RUNTIME STATE

At 2026-09-23T14:17:39Z exact-name inspection reported:

- task2-iam-keycloak-stage-a-1: absent
- task2-iam-keycloak-stage-b-1: absent
- task2-iam-diagnostic-1: absent
- task2-iam-oidc: absent
- task2-iam-keycloak-data: absent

No diagnostic container existed to remove. No Keycloak service is running.
No Task 2 data volume exists. The verified Alpine image remains in the local
image store; no image prune was performed. No runtime credential file was
created.

REMAINING P1 AND P2 PREREQUISITES

P1 must resume at the image-pull and CP1 gate. It must complete current Stage A
runtime, Windows-browser, and independent external-exposure evidence before
Stage B. It must then perform the actual mutually exclusive transition, startup
import, discovery/issuer/JWKS checks, actual client/group/role/scope inspection,
and authorization-page test.

P2 must not start until CP1 and CP2 pass. Actual ops_roles output remains a
mandatory P2 entry test, followed by the Flask callback, token validation,
ten-minute signed application session, server-side RBAC, logout/CSRF, and all
negative security tests in task2/test-plan.md.

BOUNDARY CONFIRMATION

No Kubernetes command or API access occurred.
No command or operation in this P1 execution targeted Task 1 resources or the
interrupted D3 environment.
No claim is made that Task 1 runtime was independently verified unchanged.
No DNS, hosts, firewall, Docker daemon, nftables, or iptables setting changed.
No branch/worktree, stage, commit, push, reset, rebase, merge, or stash
operation occurred.
No password, client secret, token, code, cookie, or private key is in this
record.


CONTINUATION — FINAL SINGLE-HOST RUNTIME AND P2 HANDOFF, 2026-09-24
===================================================================

LATEST DISPOSITION
------------------

TASK2-P1 SINGLE-HOST RUNTIME & KEYCLOAK VERIFIED

CP1 ORIGINAL CONTRACT:
INCOMPLETE — R8 INDEPENDENT LAN TEST DEFERRED

CP1 SINGLE-HOST EXCEPTION GATE:
PASS

CP2:
PASS

P2:
AUTHORIZED UNDER DOCUMENTED R8 EXCEPTION

INDEPENDENT LAN ISOLATION:
UNVERIFIED

ACTUAL ops_roles TOKEN OUTPUT:
UNVERIFIED — MANDATORY P2 ENTRY TEST

OPENING GIT GATE AND SOURCE REVIEW
----------------------------------

The mandatory Git gate ran before runtime or tracked-file mutation.

- actual branch: temp/work-in-progress
- actual starting HEAD:
  f7a52d63025dba62a1f7ca106e0329d6a5156971
- commit subject: wip: task2-p1-patch
- starting worktree: clean
- reviewed checkpoint:
  f7a52d63025dba62a1f7ca106e0329d6a5156971
- merge-base ancestry result: exit 0
- intervening commits: none; HEAD equaled the reviewed checkpoint

All required current Task 2 source, P0 evidence, P1 evidence, and the latest
five-file patch diff were read before runtime work. The P0 architecture and
threat model remained authoritative. No P2 implementation was performed.

SINGLE-HOST CAPABILITY ASSESSMENT
---------------------------------

First recorded current timestamp: 2026-09-23T17:45:25Z.

- PowerShell interop: OBSERVED PASS, Windows PowerShell 5.1.19041.7725
- Windows listener inspection: OBSERVED PASS
- Windows DNS inspection: OBSERVED PASS
- Windows interface inspection: OBSERVED PASS
- Windows browser capability: OBSERVED PASS, Google Chrome 153.0.8010.53
- Docker client/daemon: OBSERVED PASS, 29.3.0, linux/x86_64
- Docker Compose: OBSERVED PASS, v5.1.0
- WSL host: Linux 5.15.167.4-microsoft-standard-WSL2 x86_64
- genuinely independent authorized LAN endpoint: unavailable, consistent with
  the owner's stated single-host constraint

R8 INDEPENDENT TEST CAPABILITY:
UNAVAILABLE IN CURRENT SINGLE-HOST ENVIRONMENT

R8:
DEFERRED UNDER EXPLICIT OWNER AUTHORIZATION

STATIC CONFIGURATION REVALIDATION
---------------------------------

Before Stage A restart:

- realm JSON parse: exit 0
- rendered Compose validation: exit 0
- documented structured clientScopeMappings regression: PASS
- complete secret-safe Compose assertions: STATIC CONFIGURATION PASS
- complete secret-safe realm assertions: STATIC CONFIGURATION PASS

The source mapping outer key was exactly ops-dashboard, the target property
was clientScope, the target was ops-dashboard-roles, and roles were exactly
viewer and admin. The dedicated scope existed and was a default only for
ops-dashboard; neither disabled design-only client referenced it.

INITIAL RESOURCE AND PERSISTENT-STATE RECONCILIATION
----------------------------------------------------

Only the five authorized Task 2 identities were inspected initially.

- Stage A container: present, stopped, project task2-iam, service
  keycloak-stage-a, exact image, expected network/volume/secret mounts
- Stage B container: absent
- diagnostic container: absent
- task2-iam-oidc: present, project task2-iam, no active members
- task2-iam-keycloak-data: present, project task2-iam

The exact-volume reference query returned only the stopped Stage A container.
Prior evidence established initial volume absence, the original Stage A
creation, and realm ops HTTP 404 before shutdown. The current Stage A restart
again returned HTTP 404 for realm ops, resolving current provenance as the
documented pre-import state. No realm, volume, database, or resource was
deleted or reset.

IMAGE AND BOOTSTRAP CREDENTIAL
------------------------------

OBSERVED PASS — Keycloak image:

- reference:
  quay.io/keycloak/keycloak:26.7.4@sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf
- image ID:
  sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf
- RepoDigest matched the same immutable digest
- OS/architecture: linux/amd64
- configured image user: 1000

OBSERVED PASS — diagnostic image:

- image ID and RepoDigest:
  sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f
- OS/architecture: linux/amd64

OBSERVED PASS — existing bootstrap credential, without reading its value:

- runtime directory UID/GID/mode: 1000:1000 / 0700
- file UID/GID/mode: 1000:1000 / 0600
- file size: 65 bytes
- ignored by task2/.gitignore
- untracked
- networkless, read-only, cap-dropped probe as UID 1000:
  secret_readability=PASS
- probe container auto-removed

CURRENT STAGE A VERIFICATION
----------------------------

Stage A and diagnostic startup: OBSERVED PASS.

R1 OBSERVED PASS:

- project/service: task2-iam / keycloak-stage-a
- exact immutable image ID and configured UID 1000
- dedicated network and auth.localhost alias
- named data volume
- only 127.0.0.1:18082 published
- no import mount and command start-dev only
- no Docker socket, Task 1 attachment, privilege, or unintended host-facing
  management port
- diagnostic service was Task-2-owned, read-only, cap-dropped, unprivileged,
  without host ports or mounts

R2/R3 OBSERVED PASS: the exact Keycloak process started and used the reviewed
linux/amd64 image ID.

R4 OBSERVED PASS:

- startup logs identified Keycloak 26.7.4
- actual internal listener: http://0.0.0.0:18082
- internal master discovery HTTP/1.1 200
- WSL readiness master discovery HTTP 200
- realm ops discovery before import HTTP 404

R5 OBSERVED PASS: diagnostic DNS resolved auth.localhost to the active Stage A
container and diagnostic HTTP returned 200.

R6 OBSERVED PASS with explicit limitation: WSL curl --resolve returned HTTP
200. Ordinary WSL getent still returned no address and was not reported as DNS
success.

R7 OBSERVED PASS: Windows Chrome 153.0.8010.53 requested
http://auth.localhost:18082 and rendered the Keycloak Administration Console.
Windows DNS returned ::1 and 127.0.0.1, and the active Windows listener was
127.0.0.1:18082 only.

R9 OBSERVED PASS: neither WSL nor Windows had a listener on 18083.

R10 OBSERVED PASS: no separate management or administration port was
published. Image-declared ports other than 18082 had no host binding.

HOST-LOCAL EXPOSURE CHECKS AND R8
---------------------------------

The exact Docker binding and Windows listener were loopback-only. Bounded TCP
checks to every current preferred Windows non-loopback IPv4 interface and HTTP
checks to every current WSL global non-loopback interface did not reach port
18082. No tested non-loopback listener or unexpected exposure was observed.
Private interface addresses were not retained in this evidence.

A same-host LAN-address request, Windows interface check, WSL interface check,
container, or local VM is not an independent LAN vantage. Therefore:

R8:
DEFERRED — INDEPENDENT LAN VANTAGE UNAVAILABLE

HOST-LOCAL EXPOSURE CHECKS:
OBSERVED PASS

INDEPENDENT LAN ISOLATION:
UNVERIFIED

CP1 ORIGINAL CONTRACT:
INCOMPLETE — R8 DEFERRED

CP1 SINGLE-HOST EXCEPTION GATE:
PASS

SECURE ADMIN CLI PREFLIGHT
--------------------------

The installed Keycloak 26.7.4 kcadm.sh credential help documented
KC_CLI_PASSWORD. The verified procedure:

- disabled shell tracing before secret handling;
- applied umask 077;
- created a unique /tmp/task2-p1-kcadm.XXXXXX directory;
- used an explicit mode-0600 kcadm.config;
- read the password only from the mounted secret inside the intended container;
- exported it only as KC_CLI_PASSWORD inside that shell;
- passed no literal password as a CLI argument or Docker exec environment;
- unset it immediately after login;
- completed authenticated status and read-only master-realm GET checks; and
- used EXIT/HUP/INT/TERM handling that exited after removing only the exact
  temporary config and directory.

ADMIN CLI VERIFICATION:
OBSERVED PASS

All later Admin CLI inspections reused this pattern. Final audit found no
matching temporary directory. Client-secret presence was tested without
printing or retaining the secret.

CONTROLLED STAGE A TO STAGE B TRANSITION
----------------------------------------

Transition began at 2026-09-23T18:00:17Z.

- Stage A stopped and reported exited
- WSL and Windows ports 18082/18083 were released
- the dedicated network had no active Stage A alias owner
- the persistent volume was preserved
- only Stage B was created and started
- Stage B used the same image, network, alias, loopback publish, credential,
  and data volume
- Stage A remained stopped throughout Stage B

STAGE B IMPORT AND PROFILE-SCOPE DEFECT
---------------------------------------

Initial Stage B import: OBSERVED PASS for realm creation and the corrected
clientScopeMappings representation.

Sanitized logs reported:

- full model import requested, IGNORE_EXISTING
- Realm 'ops' imported
- import finished successfully
- Keycloak 26.7.4 listening on http://0.0.0.0:18082

The same import also warned that referenced client scope profile did not exist
and was ignored. Runtime inspection confirmed the standard profile scope was
absent. A valid authorization request with scope=openid profile, exact callback,
high-entropy state and nonce, and PKCE S256 returned a 302 error redirect with
invalid_scope. This was a reproducible source/runtime defect and O7 initially
failed.

The minimum allowlisted source correction added Keycloak 26.7.4's exact
standard profile scope representation, obtained read-only from the master
realm. The known, successfully imported ops realm was then corrected once,
without deletion or reset:

- create the version-matched profile scope through Admin REST;
- attach it to ops-dashboard through the dedicated default-client-scope REST
  endpoint; and
- verify master reference, tracked source, and live runtime representations
  were semantically equal.

The first attempted general client update did not persist the association and
was not claimed as a pass. The dedicated endpoint succeeded. No import retry,
realm deletion, volume reset, or replacement deployment occurred. Because the
existing realm must be preserved and --import-realm uses IGNORE_EXISTING, the
updated profile object was not tested through a destructive fresh-volume
import. Current source/runtime parity and actual authorization behavior were
verified; that fresh-import limitation remains explicit.

CP2 O1 THROUGH O7
-----------------

O1 OBSERVED PASS:

- diagnostic and WSL-override discovery documents were equivalent for checked
  security metadata
- issuer was exactly http://auth.localhost:18082/realms/ops

O2 OBSERVED PASS:

- authorization, token, JWKS, and end-session endpoints used the canonical
  http://auth.localhost:18082/realms/ops base
- no Docker-only hostname leaked
- Docker token endpoint returned the expected HTTP 400 to an empty request,
  proving reachability without attempting a grant
- Windows browser, Docker diagnostic, and WSL override paths were evaluated
  separately

O3 OBSERVED PASS:

- JWKS documents matched across Docker and host paths
- two public keys were returned; one was a usable RS256 asymmetric signing key
- key IDs were present and unique
- no symmetric key, alg=none key, or private key member was returned
- realm default signature algorithm was RS256 and ops-dashboard had no
  symmetric signing override
- discovery also advertised provider-wide HMAC algorithms; P2 must still
  allow-list the intended asymmetric RS256 algorithm and never infer trust from
  the broad provider capability list

O4 OBSERVED PASS:

- ops-dashboard existed and was enabled
- confidential client-secret authentication and runtime credential present
- public/bearer flags false
- code flow enabled; PKCE S256 required
- redirect exactly http://ops.localhost:18083/oidc/callback
- implicit, direct access, service account, and authorization services disabled
- Full Scope Allowed false
- profile and ops-dashboard-roles attached by default
- asset-inventory and runbook-portal existed and remained disabled

O5 OBSERVED PASS / CONFIGURED:

- six exact client roles
- four exact groups
- exact group-to-client-role mappings
- unique profile and ops-dashboard-roles scopes, plus Keycloak's built-in
  offline_access scope
- standard profile scope contained 14 version-matched mappers
- dedicated scope default only to ops-dashboard
- one User Client Role mapper with Client ID ops-dashboard
- claim ops_roles, String, multivalued
- ID-token output true
- access-token, lightweight-token, UserInfo, and introspection output false
- Full Scope Allowed false
- actual imported role-scope mapping exactly ops-dashboard:viewer and
  ops-dashboard:admin

The clientScopeMappings result above was inspected from actual Keycloak
objects; it was not inferred from source JSON.

O6 disposition:

ACTUAL ops_roles TOKEN OUTPUT:
UNVERIFIED — MANDATORY P2 ENTRY TEST

No direct-access password grant was enabled, no test client was created, and no
raw token was obtained or retained. P2 must prove viewer ["viewer"], admin
exactly viewer and admin, multivalued-array shape, and unrelated-role exclusion
before trusting the claim.

O7 OBSERVED PASS after the profile correction: Windows Chrome
153.0.8010.53 rendered title "Sign in to SecOps Operations" and the realm login
form for a valid scope=openid profile authorization-code request with the exact
redirect, 32-byte state, 32-byte nonce, and PKCE S256. The temporary profile
was removed. No callback or application SSO was claimed.

CP2:
PASS

OPTIONAL RESTART / IDEMPOTENCY
------------------------------

Restart began at 2026-09-23T18:46:44Z.

- only Stage B stopped
- WSL and Windows listeners were absent while stopped
- the same Stage B container and volume restarted
- import logs reported Realm 'ops' already exists. Import skipped
- discovery and JWKS continued passing
- clients, runtime credential presence, six roles, four groups, exact group
  mappings, three unique scopes, 14 profile mappers, dedicated mapper, default
  attachments, and exact viewer/admin role-scope mapping remained intact
- no duplication or unexpected overwrite was observed

RESTART/IDEMPOTENCY:
PASS

MANDATORY CLEANUP AND FINAL RUNTIME STATE
-----------------------------------------

- all Admin CLI temporary configurations: removed; final audit NONE
- all Task-2 temporary Chrome profiles/processes: removed; final audit NONE
- diagnostic container: stopped and removed
- Stage A container: retained, stopped, correct project/service/image
- Stage B container: retained, stopped, correct project/service/image
- task2-iam-oidc: retained, correct project, no active members
- task2-iam-keycloak-data: retained, correct project, provisioned ops state
- bootstrap credential: retained, ignored, untracked, UID/GID 1000:1000,
  mode 0600, 65 bytes
- final WSL listeners on 18082/18083: absent
- final Windows listeners on 18082/18083: absent

The current volume is no longer a pre-import volume. It contains the imported
and verified ops realm plus the documented profile-scope correction. It was
not deleted, reset, or recreated. Stage B is intentionally stopped for the P2
handoff and can be restarted with:

docker compose --project-directory task2 --profile stage-b up -d
keycloak-stage-b

REMAINING RISKS AND P2 ENTRY CONDITIONS
---------------------------------------

- Independent LAN isolation remains UNVERIFIED; R8 is deferred, not passed.
- Local HTTP, start-dev, and the embedded database remain POC-only limitations.
- The corrected profile object has source/runtime parity but was not exercised
  through a prohibited destructive fresh-volume re-import.
- Actual emitted ops_roles content remains UNVERIFIED and is the first mandatory
  P2 integration test.
- P2 still owns Flask, callback processing, complete token validation,
  sessions, RBAC, CSRF, logout, and integrated negative testing.

P2 AUTHORIZATION DECISION
-------------------------

P2:
AUTHORIZED UNDER DOCUMENTED R8 EXCEPTION

This authorization depends on the single-host gate and CP2 results above. It
does not claim that the original CP1 contract passed or that all LAN devices
are unable to reach the service.

BOUNDARY CONFIRMATION
---------------------

No P2 application was created. No Task 1 or Kubernetes command ran. No
unrelated Docker resource was inventoried, stopped, attached, removed, or
pruned. No firewall, Docker daemon, WSL network, DNS, hosts, nftables, or
iptables setting changed. No branch/worktree, Git stage, commit, push, reset,
merge, rebase, or stash operation occurred. No password, client secret, raw
token, authorization code, cookie, private key, or unrestricted sensitive
Admin CLI output was retained in this record.

FINAL VALIDATION
----------------

Observed at 2026-09-23T19:06:46Z.

- python3 -m json.tool task2/keycloak/ops-realm.json: exit 0
- docker compose --project-directory task2 --profile '*' config --quiet:
  exit 0
- structured clientScopeMappings and profile-scope regression: PASS
- complete Compose model assertions: STATIC CONFIGURATION PASS
- complete realm model assertions: STATIC CONFIGURATION PASS
- git diff --check: exit 0, no output
- git diff --cached --name-only: empty
- runtime credential: ignored and untracked
- git status and git diff --name-only returned exactly the five allowlisted
  tracked paths:
  - docs/evidence/task2/t2-p1-execution.txt
  - docs/evidence/task2/t2-p1-scope-record.txt
  - task2/README.md
  - task2/keycloak/ops-realm.json
  - task2/test-plan.md

Nothing was staged, committed, or pushed.


CONTINUATION — SEMANTIC PATCH AND SAFE RUNTIME, 2026-09-23
==========================================================

CONTINUATION DISPOSITION
------------------------

TASK2-P1 PARTIAL —
RUNTIME OR HOST EXPOSURE GATE PENDING

CP1: INCOMPLETE / BLOCKED
CP2: NOT RUN
P2: NOT AUTHORIZED TO START

R1 through R7, R9, and R10 have current observed evidence. R8 is
INCONCLUSIVE because no separate LAN device or technically equivalent
independent external vantage point was available, so no connectivity control
or independent request to the Windows LAN address could be performed. Per the
mandatory pause rule, Stage B was not started.

CONTINUATION WINDOW AND OPENING GATE
------------------------------------

Continuation date: 2026-09-23 UTC
Opening gate ran before the first recorded continuation timestamp
2026-09-23T16:24:43Z; its exact UTC instant was not separately captured.

Actual starting branch: temp/work-in-progress
Actual starting HEAD: a641aeb5588a5d282ed0cbfaaf1aacd5c1c390a5
Commit subject: wip: task2-p1
Reviewed checkpoint: a641aeb5588a5d282ed0cbfaaf1aacd5c1c390a5
Starting git status --short --untracked-files=all: <empty; clean>
Ancestry command:
git merge-base --is-ancestor
a641aeb5588a5d282ed0cbfaaf1aacd5c1c390a5 HEAD
Exit status: 0
Intervening commits: none; HEAD was the reviewed checkpoint.

REQUIRED SOURCE REVIEW
----------------------

OBSERVED PASS. The continuation read every required current Task 2 source and
P0/P1 evidence file, reviewed the P1 delta from
1fea0957be5437948467d6e9470418d1d7523810, and read the four-page assessment
PDF locally. The PDF SHA-256 remained:
bf509a32052527444b4fc75c7492c263705606c66fe01e0ce4ff097958c2f6de

The official assessment still requires Option A to design centralized IAM for
two or three internal applications and demonstrate OIDC/SSO for at least one.
No P2 work or architecture redesign was authorized or performed.

SEMANTIC CORRECTION
-------------------

Exact source correction in task2/keycloak/ops-realm.json:

-        "client": "ops-dashboard-roles",
+        "clientScope": "ops-dashboard-roles",

Every unrelated realm field was preserved.

The copy-pasteable structured regression check was added to
task2/test-plan.md. It asserted the exact outer client key, exact
clientScope/roles entry shape, exact scope target, exact viewer/admin role set,
scope existence, default attachment only to ops-dashboard, and absence from
the two disabled design-only clients.

Actual result:
PASS: declared client-scope mapping
Exit status: 0
Classification: STATIC CONFIGURATION PASS

This is source validation. It is not a successful import, observed Keycloak
state, or emitted-token evidence.

COMPLETE STATIC REVALIDATION
----------------------------

python3 -m json.tool task2/keycloak/ops-realm.json >/dev/null
Exit status: 0

docker compose --project-directory task2 --profile '*' config --quiet
Exit status: 0

Secret-safe structured assertions returned:
STATIC CONFIGURATION PASS: Compose model
STATIC CONFIGURATION PASS: realm model
Exit status: 0

The assertions covered the exact project, three services, immutable images,
linux/amd64 platform, sole loopback publish, common network/alias/volume,
mutually exclusive Stage A and Stage B commands, read-only Stage B import
mount, bounded unprivileged diagnostic, no Flask or Task 1 reference, exact
realm/clients/roles/groups/default scope/mapper/mapping, and absence of a
tracked realm credential.

INITIAL RUNTIME AND PERSISTENT-STATE RECONCILIATION
---------------------------------------------------

Before secret creation or Docker resource creation, exact-name inspection
returned all five intended identities absent:

- task2-iam-keycloak-stage-a-1
- task2-iam-keycloak-stage-b-1
- task2-iam-diagnostic-1
- task2-iam-oidc
- task2-iam-keycloak-data

This established a new-volume Stage A baseline. No existing or unknown
persistent state was reused or deleted.

CURRENT HOST PREFLIGHT
----------------------

Recorded at 2026-09-23T16:24:43Z:

- Docker client: 29.3.0
- Docker Compose: v5.1.0
- Docker daemon: 29.3.0, linux/x86_64
- Host: Linux 5.15.167.4-microsoft-standard-WSL2 x86_64
- WSL listeners on 18082/18083: absent before startup
- Windows listeners on 18082/18083: absent before startup
- Windows auth.localhost and ops.localhost DNS: ::1 and 127.0.0.1
- ordinary WSL auth.localhost and ops.localhost DNS: exit 2, no result

No DNS, hosts, firewall, WSL, or Docker daemon setting was modified.

IMAGE ACQUISITION AND INTEGRITY
-------------------------------

At the current preflight the exact Keycloak image, absent at the end of the
historical execution above, was locally inspectable. One bounded command then
confirmed acquisition:

timeout --signal=TERM --kill-after=15s 300s docker pull --platform linux/amd64
quay.io/keycloak/keycloak:26.7.4@sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf

Actual result: Image is up to date
Exit status: 0

OBSERVED PASS — exact local Keycloak image:
- Image ID: sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf
- RepoDigest: quay.io/keycloak/keycloak@sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf
- OS/architecture: linux/amd64
- configured image user: 1000
- reviewed multiarchitecture index digest retained from the prior verification:
  sha256:82a77884f3af238beab1e7afd63b5f530e1b5c0590bd7aa60b40a40463e29b2c

OBSERVED PASS — exact local diagnostic image:
- Image ID: sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f
- RepoDigest: alpine@sha256:eafc1edb577d2e9b458664a15f23ea1c370214193226069eb22921169fc7e43f
- OS/architecture: linux/amd64
- configured image user: empty/default

No alternate tag, digest, or platform and no Docker-wide cleanup was used.

BOOTSTRAP SECRET
----------------

The approved file was initially absent and was confirmed ignored and
untracked. The prior README spelling
"openssl rand -base64 48 -out ..." failed with this host's OpenSSL argument
parser before creating the file. The supported equivalent was used and the
README was corrected:

openssl rand -base64 -out
task2/.runtime/keycloak-bootstrap-admin-password 48

Observed metadata without reading or printing the value:
- directory UID/GID/mode: 1000:1000 / 0700
- file UID/GID/mode: 1000:1000 / 0600
- file size: 65 bytes
- git check-ignore: matched task2/.gitignore
- git ls-files --error-unmatch: exit 1, untracked

An isolated, networkless, read-only, cap-dropped container using the exact
Keycloak image ran as UID 1000, tested readability and nontrivial byte length,
printed no value, returned secret_readability=PASS, and was auto-removed.
Exit status: 0.

STAGE A EXECUTION
-----------------

Start time: 2026-09-23T16:32:17Z
Command:
docker compose --project-directory task2 --profile stage-a
--profile diagnostic up -d keycloak-stage-a diagnostic
Exit status: 0

OBSERVED PASS — runtime identity and ownership:
- Stage A container ID prefix: ed9840e03b07
- exact image ID: sha256:3d911baa186f352563854039b95f21a7e2c01c76b527fdc64f24a0885b927bdf
- Compose project/service: task2-iam / keycloak-stage-a
- command: start-dev; no --import-realm
- network/alias: task2-iam-oidc / auth.localhost
- volume: task2-iam-keycloak-data at /opt/keycloak/data
- import mount: absent
- publish: 127.0.0.1:18082 -> 18082/tcp only
- diagnostic project/service: task2-iam / diagnostic
- diagnostic mounts and host ports: none
- diagnostic privileged: false; root filesystem read-only

The log reported Keycloak 26.7.4 started in development mode and listening on
http://0.0.0.0:18082. It initialized the master realm and temporary bootstrap
administrator without printing the password.

R4 OBSERVED PASS:
- bounded WSL readiness poll reached master discovery with HTTP 200
- an HTTP/1.1 request from inside the Keycloak container to its internal
  listener returned HTTP/1.1 200 OK
- the host request for realm ops discovery returned HTTP 404, proving realm
  ops was absent in this newly created pre-import state

R5 OBSERVED PASS:
- diagnostic DNS resolved auth.localhost to active Stage A address 172.19.0.3
- diagnostic HTTP reached master discovery and returned HTTP/1.1 200 OK

R6 OBSERVED PASS with DNS limitation recorded separately:
- WSL curl --resolve canonical-host request returned HTTP 200
- ordinary WSL getent for auth.localhost exited 2 with no result
- ss showed only 127.0.0.1:18082; port 18083 remained absent

R7 WINDOWS-BROWSER OBSERVED PASS:
- active Windows listener: 127.0.0.1:18082 only
- Windows auth.localhost DNS: ::1 and 127.0.0.1
- first Edge headless attempt failed before usable page evidence with Windows
  error 577 and was not counted as a pass
- Google Chrome version 153.0.8010.53 opened
  http://auth.localhost:18082 using a unique temporary profile
- browser exit: 0
- rendered title: Sign in to Keycloak
- rendered DOM contained the Keycloak marker
- the unique Chrome profile was removed; no Task 2 browser process remained

R8 INCONCLUSIVE / NOT RUN:
No independent LAN device or equivalent external vantage was accessible.
Therefore no Windows-LAN-IP request and no approved connectivity control were
performed. A same-host, WSL, or arbitrary timeout was not promoted to evidence.

R9 OBSERVED PASS: port 18083 had no WSL or Windows listener.

R10 OBSERVED PASS: only 18082/tcp was published on Windows/WSL loopback; no
separate management or administration port was published.

CP1: INCOMPLETE / BLOCKED solely on current independent R8 evidence.

OWNER-ASSISTED RESUMPTION REQUIREMENT
------------------------------------

On resume, historical Stage A evidence does not pass a new checkpoint. Recheck
the exact container/image/volume state, restart Stage A and diagnostic, and
repeat readiness, binding, diagnostic, WSL, Windows browser, and exposure
checks. From a genuinely separate LAN device, first establish an approved
non-sensitive connectivity control to the Windows host or its LAN (for
example, an ARP response on the same subnet or an already approved existing
service), then use a bounded request to
http://<WINDOWS_LAN_IP>:18082. Do not change firewall or expose Task 2 to make
the control work. Redact the private address in public evidence.

STAGE B AND CP2
---------------

Stage A to Stage B transition: NOT RUN
Stage B startup/import: NOT RUN
Partial import concern: none; realm ops was never imported
Admin CLI authentication/configuration: NOT RUN
Temporary Admin CLI state created: none
O1 discovery for realm ops: NOT RUN
O2 endpoint consistency: NOT RUN
O3 JWKS: NOT RUN
O4 actual client state: NOT RUN
O5 actual roles/groups/scope/mapper/mapping: NOT RUN
Actual imported client-scope mapping: UNVERIFIED / NOT RUN
O7 authorization page for realm ops: NOT RUN
RESTART/IDEMPOTENCY: NOT VERIFIED

ACTUAL ops_roles TOKEN OUTPUT:
UNVERIFIED — MANDATORY P2 ENTRY TEST

CP2: NOT RUN
P2: NOT AUTHORIZED TO START

MANDATORY PAUSE CLEANUP AND FINAL RUNTIME STATE
------------------------------------------------

Cleanup began at 2026-09-23T16:47:56Z.

- task2-iam-diagnostic-1: stopped and removed
- task2-iam-keycloak-stage-a-1: stopped, retained, Compose ownership verified
- task2-iam-keycloak-stage-b-1: absent
- task2-iam-oidc: retained, Compose project task2-iam
- task2-iam-keycloak-data: retained, Compose project task2-iam
- final WSL and Windows listeners on 18082/18083: absent
- Task 2 browser processes and temporary browser directories: none
- temporary secret-readability container: auto-removed
- temporary Admin CLI configuration: none created

The final volume is a documented pre-import Stage A state created from initial
absence. It contains initialized master state, while actual HTTP evidence showed
realm ops absent before Stage A was stopped. It was not deleted, reset, or
repurposed.

BOUNDARY CONFIRMATION FOR CONTINUATION
--------------------------------------

No Kubernetes or Task 1 command was run. No unrelated Docker identity was
inspected, stopped, attached, or removed. No Docker prune ran. No DNS, hosts,
firewall, WSL, Windows, or Docker daemon setting changed. No credential, token,
authorization code, cookie, client secret, private key, or unrestricted Admin
CLI response was printed or written to evidence. No Git stage, commit, push,
reset, rebase, merge, or stash operation occurred.
