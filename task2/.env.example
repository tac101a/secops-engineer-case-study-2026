# Optional non-secret local override. Copy to .env only when a different
# bootstrap administrator username is required.
KEYCLOAK_BOOTSTRAP_ADMIN_USERNAME=replace-with-local-admin-username

# Generate the credential consumed by Compose separately; never place it here:
#   .runtime/keycloak-bootstrap-admin-password
